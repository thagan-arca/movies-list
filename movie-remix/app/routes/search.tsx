// import { Outlet } from "@remix-run/react";

import { Outlet } from "@remix-run/react";

function SearchRoute() {
    return (
        <div>
            <Outlet />
        </div>
    );
}
export default SearchRoute;
